# 05-23 SQL 优化记录

> 项目：`dotnet-90days-bootcamp / InprovePlan`
>
> 日期：`YYYY-MM-DD`
>
> 目标：沉淀一次“慢 SQL 定位 -> 根因分析 -> 优化 -> 验证”的完整工程记录。

## 1. 问题标题

`[填写标题]`

## 2. 背景

说明问题来源、触发方式、影响范围、为什么怀疑是 SQL 问题。

## 3. 触发场景

- 接口：
- 查询条件：
- 数据量背景：
- 环境：
- 说明：

## 4. 现象证据

### 4.1 请求现象（优化前）

- TraceId：
- 路径：
- 状态码：
- DurationMs：

日志证据：

```json
[填写 started / completed 日志]
```

### 4.2 请求现象（优化后）

- TraceId：`[填写 TraceId]`
- 路径：`[填写接口路径]`
- 状态码：`[填写状态码]`
- DurationMs：`[填写耗时]`

日志证据：

```json
[填写 started / completed 日志]
```

### 4.3 接口响应摘要

- 返回条数：`[填写返回条数]`
- SQL 次数：`[填写 SQL 次数]`
- 查询语义是否一致：`[填写 是/否]`

说明：
`[填写补充说明，例如：单次接口总耗时可能受环境波动影响，因此仍需结合 EXPLAIN 判断]`

## 5. 目标 SQL

```sql
[填写目标 SQL]
```

## 6. 执行计划分析

### 6.1 优化前

- `type = [填写]`
- `possible_keys = [填写]`
- `key = [填写]`
- `rows = [填写]`
- `Extra = [填写]`

结论：
`[填写优化前执行计划结论]`

### 6.2 优化后

- `type = [填写]`
- `possible_keys = [填写]`
- `key = [填写]`
- `rows = [填写]`
- `Extra = [填写]`

结论：
`[填写优化后执行计划结论]`

## 7. 根因分析

本次慢 SQL 的根因是：

`[填写根因，例如：缺少 UserId 条件列索引，导致查询退化为全表扫描]`

证据链如下：

1. `[填写证据 1]`
2. `[填写证据 2]`
3. `[填写证据 3]`
4. `[填写证据 4]`
5. `[填写证据 5，可选]`
6. `[填写证据 6，可选]`

因此可以确认：
`[填写最终根因判断]`

## 8. 优化动作

本次优化动作如下：

1. `[填写动作 1]`
2. `[填写动作 2]`
3. `[填写动作 3，可选]`

优化目标：
`[填写优化目标，例如：降低扫描行数，使执行计划命中过滤列索引]`

## 9. 优化效果

### 9.1 执行计划变化

优化前：
`[填写优化前摘要，例如：全表扫描，type = ALL，key = null，rows = 96470]`

优化后：
`[填写优化后摘要，例如：索引命中，type = ref，key = IX_app_orders_UserId，rows = 360]`

### 9.2 结果一致性

- 返回条数一致：`[填写]`
- 查询条件一致：`[填写]`
- 关键字段一致：`[填写]`
- 业务语义一致：`[填写]`

### 9.3 性能说明

- 优化前：`[填写优化前耗时]`
- 优化后：`[填写优化后耗时]`

说明：
`[填写说明，例如：接口总耗时与 SQL 执行耗时并不完全等价，因此本次以 EXPLAIN、索引命中和扫描行数变化作为主证据]`

## 10. 风险与限制

1. `[填写风险 1]`
2. `[填写风险 2]`
3. `[填写风险 3]`
4. `[填写风险 4，可选]`

## 11. 复现步骤

1. `[填写步骤 1]`
2. `[填写步骤 2]`
3. `[填写步骤 3]`
4. `[填写步骤 4]`
5. `[填写步骤 5]`
6. `[填写步骤 6，可选]`

## 12. 关联提交

- `[commit] | [date] | [message]`

## 13. 结论

- 是否完成定位：`[填写]`
- 是否完成优化：`[填写]`
- 是否达到投产标准：`[填写]`

结论说明：
`[填写总结，例如：已完成慢 SQL 定位与索引优化验证，但若按更严格生产标准，仍建议补多次重复测试并输出平均值]`
